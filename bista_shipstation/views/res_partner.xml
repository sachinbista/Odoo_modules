<odoo>
    <record id="res_partner_inherit_form" model="ir.ui.view">
        <field name="name">Partner Form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <field name="vat" position="after">
                <field name="third_part_account"/>
                <field name="ship_via"/>
            </field>
            <xpath expr="//notebook" position="inside">
                <page string="Shipstation Third Party Account"
                      attrs="{'invisible': [('third_part_account','=',False)]}">
                    <group>
                        <field name="bill_account"/>
                        <field name="bill_postal_code"/>
                        <field name="bill_country_code"/>
                        <field name="carrier_id"/>
                        <field name="service_id" context="{'carrier_service_id':carrier_id}"/>
                    </group>
                </page>
            </xpath>

        </field>
    </record>


    <record id="sale_order_inherit_form" model="ir.ui.view">
        <field name="name">Sale Order Form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="is_free_shipping" string="No Shipping Cost Sync"
                       attrs="{'readonly': [('state', 'in',('sale','done'))]}"/>
                <field name="ship_via"/>
            </xpath>
        </field>
    </record>
</odoo>