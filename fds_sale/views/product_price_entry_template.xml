<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <template id="product_price_entry_modal" name="Product Price Entry">
        <main class="modal-body">
            <h4>If a product has both MTO and Buy routes, add popup in Sales order form,  to require price entry - these will essentially be special order items that’s price needs checked since it changes from the vendor but is not actively updated in Odoo.  This will happen on the sales order form, when the product is selected on the product line. </h4>
            <table class="table table-striped table-sm">
                <thead>
                    <tr>
                        <th class="td-img"><span class='label'>Product</span></th>
                        <th><span class='label'></span></th>
                        <th class="text-center td-price"><span class='label'>Price</span></th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="js_product in_cart main_product">
                        <input type="hidden" class="product_template_id" t-att-value="product_template.id"/>
                        <input type="hidden" class="product_id" name="product_id" t-att-value="product_id"/>
                        <td class='td-img'>
                            <img class="product_detail_img" t-att-src="'/web/image/product.template/%s/image_128' % product_template.id" alt="Product Image"/>
                        </td>
                        <td class='td-product_name'>
                            <strong class="product-name product_display_name" t-esc="product_template.name"/>
                            <div class="text-muted small">
                                <div t-field="product_template.description_sale"/>
                                <div class="js_attributes"/>
                            </div>
                        </td>
                        <td class="text-center td-price" name="price">
                            <input class="js_price_unit form-control price_unit o_field_float o_field_number o_field_widget o_quick_editable o_input o_required_modifier" name="price_entry" t-att-value="price_entry"/>
                        </td>
                    </tr>
                </tbody>
            </table>
        </main>
    </template>

</odoo>
