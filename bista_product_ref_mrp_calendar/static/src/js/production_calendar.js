odoo.define('bista_product_ref_mrp_calendar.calendar_modification', function (require) {
    "use strict";

    const { calendarView } = require('@web/views/calendar/calendar_view');

    // console.log(calendarView)
    //
    // calendarView.include({
    //     render_event: function (event) {
    //         var $event = this._super.apply(this, arguments);
    //
    //         // Access additional_field from the event data and display it
    //         var additionalField = 'Test';
    //         $event.find('.o_calendar_event_content .o_calendar_event_title').text(additionalField);
    //
    //         return $event;
    //     },
    // });

});